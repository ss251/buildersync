{
  "name": "@elizaos/plugin-bootstrap",
  "version": "0.2",
  "main": "dist/index.js",
  "type": "module",
  "types": "dist/index.d.ts",
  "exports": {
    "./package.json": "./package.json",
    ".": {
      "import": {
        "@elizaos/plugin-bootstrap": "./src/index.ts",
        "types": "./src/index.ts",
        "default": "./dist/index.js"
      }
    }
  },
  "dependencies": {
    "@elizaos/core": "workspace:*"
  },
  "devDependencies": {
    "tsup": "^8.3.5"
  },
  "scripts": {
    "build": "tsup --format esm --dts",
    "dev": "tsup --format esm --dts --watch",
    "start": "tsup --format esm --dts",
    "lint": "eslint . --fix"
  },
  "peerDependencies": {}
}
